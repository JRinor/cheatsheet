# Kali Docker Commands

To start the Kali container:
```bash
sudo docker start kali-container
```

To execute a bash shell inside the running Kali container:
```bash
sudo docker exec -it kali-container bash
```
